{
  "name": "checkbourses",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js"
  },
  "dependencies": {
    "feed-watcher": "*",
    "nodemailer": "^4.7.0"
  }
}
